/*const nome = "EF_ohh!"; // In "js" you may or may not use ";" to end an instruction.
console.log(name)       //  Check installations on the Ctrl+J console, node index.js, node -v, np, -v, choco -v*/

// Create a program that averages grades between students and sends an avarege score message.

const name = "Mayk"
const name = 'Diego'
const name = `Valeska e ${name}`// Template string (You can insert a variable by calling another variable).

console.log(name3)